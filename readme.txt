While true
    Show prompt
    Read user input from console
    if user is a valid move ("r", "p", "s")
        Choose the AI to move at random
        Compare the player move with the AI move
        Show the result
    else if input is "q"
        Quit the Program
    else
        Invalid input
End 
